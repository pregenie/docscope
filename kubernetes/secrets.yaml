# Example secrets file - DO NOT commit actual secrets to version control!
# Use a secret management tool like Sealed Secrets, Vault, or cloud provider secret managers
apiVersion: v1
kind: Secret
metadata:
  name: docscope-secrets
  namespace: docscope
  labels:
    app: docscope
type: Opaque
stringData:
  database-url: "postgresql://docscope:changeme@postgres:5432/docscope"
  redis-url: "redis://redis:6379/0"
  secret-key: "change-this-secret-key-in-production"
  jwt-secret: "change-this-jwt-secret-in-production"